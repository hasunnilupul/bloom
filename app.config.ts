export default defineAppConfig({
  ui: {
    primary: "yellow",
    notifications: {
      // Show toasts at the top right of the screen
      position: "bottom-2 sm:top-0 sm:right-0 sm:bottom-auto",
    },
  },
});
